//import {Exercise} from "../types/types";

const Total = (/*{ courseParts }: { courseParts: Array<Exercise> }*/) => (
    <p></p>
    /*<p>
        Number of exercises{" "}
        {courseParts.reduce((carry, part) => carry + part.exerciseCount, 0)}
    </p>*/
);

export default Total